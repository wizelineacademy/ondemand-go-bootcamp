package service

// import (
// 	// "errors"
// 	"testing"

// 	db "pas.com/v1/infrastructure/database"
// )

// func TestServiceSuccess(t *testing.T) {
// 	var data [][]string
// 	store := db.NewStore(&data)

// 	//service := service.NewService(store)
// 	//records := store.ReadData("data.csv")
// 	if len(records) != 0 {
// 		t.Errorf("Expected: %v, got: %v", 0, len(records))
// 	}
// }
